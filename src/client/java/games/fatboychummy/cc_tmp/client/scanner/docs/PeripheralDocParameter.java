package games.fatboychummy.cc_tmp.client.scanner.docs;

public record PeripheralDocParameter(
        String name,
        String description,
        String shortDescription,
        String luaType
) {}
